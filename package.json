{
  "name": "livraria-pagina-nova-aula",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
      "postinstall": "if [ $CLIENT_ENV ]; then npm run postinstall-client; elif [ $SERVER_ENV ]; then npm run postinstall-server; else echo no environment detected, please set CLIENT_ENV or SERVER_ENV; fi",
      "postinstall-client": "cd frontend && npm install && npm run build",
      "postinstall-server": "cd backend && npm install"
  },
  git remote add front-heroku https://git.heroku.com/app-frontend-nat.git
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cloudinary": "^1.30.0",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^2.3.3",
    "sequelize": "^6.20.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.16",
    "sequelize-cli": "^6.4.1"
  }
}
